# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias all
  members *
}

define hostgroup {
  hostgroup_name Linux
  alias Linux
}

define hostgroup {
  hostgroup_name sites
  alias sites
}

define hostgroup {
  hostgroup_name external_sites
  alias External Sites
}

<% @roles.each do |r| -%>
define hostgroup {
  hostgroup_name <%= r %>
  alias <%= r %>
}

<% end -%>
