#!/bin/bash
#
# Generated by Chef
# Changes will be overwritten
#

tmp="/tmp/check_procs-$(date +%s).tmp"
args=$@

/usr/lib/nagios/plugins/check_procs ${args} > $tmp
status=`echo $?`

results=`cat $tmp`
perf=`cat $tmp | awk '{ print $3 }'`

echo -n "$results "
echo -n '|'
echo "procs=$perf"

rm -f $tmp
exit $status
